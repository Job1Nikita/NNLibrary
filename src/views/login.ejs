<%- include("partials/head", { title }) %>

<main class="auth-shell">
  <section class="auth-card">
    <h1><%= t("auth.loginTitle") %></h1>
    <p class="muted"><%= t("auth.loginLead") %></p>

    <% if (flash) { %>
      <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <% } %>

    <form method="post" action="/login" class="stack-form">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <label>
        <%= t("auth.login") %>
        <input type="text" name="login" minlength="3" maxlength="32" pattern="[a-zA-Z0-9_.-]+" required autocomplete="username" />
      </label>
      <label>
        <%= t("auth.password") %>
        <input type="password" name="password" minlength="8" maxlength="128" required autocomplete="current-password" />
      </label>

      <%- include("partials/captcha") %>

      <button type="submit" class="btn"><%= t("auth.loginSubmit") %></button>
    </form>

    <p class="muted"><%= t("auth.noAccount") %> <a href="/register"><%= t("auth.goRegister") %></a></p>
  </section>
</main>

<script src="/public/js/captcha.js" defer></script>
<%- include("partials/foot") %>
